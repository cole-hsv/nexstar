'use strict';

var assert = require('assert');
var nexstar = require('../lib');

describe('nexstar lib', function () {
    it('nexstar should exist from ../lib', function () {
        assert(nexstar);
    });
});